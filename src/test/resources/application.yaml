spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/romashko_db_test
    username: crypto_tracker
    password: crypto_tracker
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true